# CLAUDE.md

このファイルは、このリポジトリでコードを扱う際にClaude Code (claude.ai/code) へ向けたガイダンスを提供します。

## これは何か

聖徳太子 (Shotoku Taishi) — 日本の保険営業担当者向けの、単一ファイル構成の音声録音・文字起こし・要約Webアプリです。商談を録音すると、文字起こし(OpenAI Audio Transcription API)と要約(OpenAI Chat Completions API)を行い、提出可能な報告書に仕上げます。すべてクライアントサイドで完結します。バックエンドは存在しません:APIリクエストはユーザー自身のAPIキーを使ってブラウザから直接OpenAIへ送られ、音声と履歴はブラウザ内(IndexedDB / localStorage)に保存され、アプリが管理するサーバーへ送信されることは一切ありません。

`main`から直接GitHub Pagesでデプロイされています(https://takoyaki-project.github.io/shoutoku-taishi/) — ビルドステップ、バンドラー、フレームワークはありません。

## リポジトリ構成

- `index.html` — アプリケーション全体(約2,600行):インラインの`<style>`、インラインの`<script>`(1つのIIFE)、外部のJS/CSSファイルなし、依存関係なし。
- `tests/smoke.test.js` — 唯一のテストファイル。素の`node`で実行する(テストフレームワークなし、npmなし、`package.json`なし)。
- `README.md` — ユーザー向けのプロダクト説明(日本語)。
- `shotokutaishi-*.md` — 過去の機能追加・修正依頼の単発指示書(それぞれが特定の変更内容を記述しており、多くはすでに実装・コミット済み)。これらはスクラッチ/過去のタスク概要であり、生きた仕様書ではありません — ユーザーが特定のものを指定しない限り、現行の要件として扱わないこと。意図的にuntracked/未コミットのまま置かれています。
- `shotoku-taishi-ui-implementation/` — トップ画面再設計についての、同様の古い単発実装指示書。

## コマンド

ビルド・バンドル・lint・開発サーバーのいずれのステップもありません — `index.html`がそのままデプロイされる成果物です。

**`index.html`編集後の構文チェック**(インラインスクリプトを取り出してパースする):
```bash
node -e "
const fs = require('fs');
const html = fs.readFileSync('index.html','utf8');
const m = html.match(/<script>([\s\S]*)<\/script>/);
new Function(m[1]);
console.log('syntax OK');
"
```

**テストスイートの実行:**
```bash
node tests/smoke.test.js
```

**見た目・挙動の検証**(UIをカバーするテストフレームワークは存在しない):`playwright-core`をその場でインストールし、headless Chromiumで`file://`経由でページを操作してスクリーンショットを撮り、終わったら`playwright-core`/`node_modules`/lockfileを再び削除する — このリポジトリにNodeの依存関係がコミットされていないのは意図的な状態です。`tests/smoke.test.js`は純粋なロジック関数しかカバーしていないため、CSS・レイアウト・アニメーションの変更や、一連の操作フロー(録音→停止→トリミング→確定)が実際に動くことを、変更完了として報告する前に確認する確立された方法です。

## アーキテクチャ

### 1つのスクリプト、1つのIIFE、1つの「設定ゾーン」

すべてのJSは`index.html`末尾にある単一の`(() => { ... })()`の中にあります。そのIIFEの先頭付近に、明示的にマークされたブロックがあります:

```
// ============================================================
// 設定ゾーン: 配布先ごとにカスタマイズするのはこのブロックだけ
// ============================================================
```

ここには`COST_CONFIG`(アプリ内のコスト概算に使う分単価/トークン単価)、`REPORT_CONFIG.systemPrompt`(商談要約のプロンプト/フォーマット)、`MEMO_CONFIG.systemPrompt`(「メモ」モード選択時に使う汎用メモフォーマット)、そして`DICTIONARY_CONFIG`(後述)が含まれます。ここは、アプリの出力フォーマットや語彙を調整する際に編集することを想定した唯一の場所です — 「アプリの配管部分」と「内容・フォーマット」の境目として扱い、フォーマットの変更はファイルの他の部分に散らすのではなく、まずここを編集することを優先してください。

### 3層構造の誤変換補正辞書 (`DICTIONARY_CONFIG`)

保険業界の専門用語は、汎用の音声認識では崩れやすくなっています。これに対応するため、異なる段階で適用される3つの層があります:
1. **`whisperPrompt`** — 文字起こしAPIの出力に影響を与えるために送信できるヒント文字列(現在は`useWhisperPrompt: false`により無効化されている。gpt-4o-mini-transcribeはwhisper-1のようにはこれで恩恵を受けないため — このフラグのコメントを参照)。
2. **`exactReplace`** — 曖昧さのない機械的な置換を行う、単純な`{wrong: right}`マップ。各チャンクが文字起こしされた直後(`sendChunk`内)に`applyExactReplace()`で適用される。文脈に関わらず100%誤りであるものにのみ使う。
3. **`contextNotes` + `llmGlossary`** — 要約用のLLMに渡され、単純な置換では直せない曖昧な同音異義語(例:保障/補償/保証)を文脈から判断させるために使う。

これとは別に、ユーザーが編集可能な**個人辞書**(localStorage内の`USER_DICT` / `koememo_user_dict`、設定画面で管理)が、`applyUserDictReplace()`によって`exactReplace`の上に重ねて適用されます — 共有辞書に入れるべきではない、顧客名など担当者ごとの語彙のためのものです。`exactReplace`とは異なり、「商談」「メモ」両方のモードで適用されます。

### 画面の状態はルーティングではなく`<body>`のクラスで駆動される

ルーターは存在しません。各画面は、`<body>`に付け外しされるクラスをキーとしたCSSの表示切替です(`<style>`内の`body.session-live`、`body.trim-review`、`body.record-done`の各ブロックを参照):
- `session-live` — 録音セッションがアクティブな間(録音中・一時停止中・停止後の処理中を含む)全般:同意・モードのステップと履歴を隠し、録音UIを表示する。
- `dim-mode` — 電池節約用の黒画面。実際に録音している間だけ付与され、トリミング画面に入る前、録音停止の瞬間に取り除かれる。
- `trim-review` — 停止後の「送信範囲を確認」画面。`stop()`内で(非同期のチャンク確定処理より前に)同期的に付与される。これは停止からトリミング画面表示までの間に「処理中」UIが一瞬映り込むのを避けるためなので、この順序は変更しないこと。
- `record-done` — 「記録が完成しました」を表示する全画面ビュー。履歴項目の詳細ポップアップ(`showCompletedRecord()`)としても再利用されている。

`clockCard`にはこれに加えて、フェーズに応じて`.rec` / `.disabled` / `.processing`が付与されます。

### クラッシュ・中断への耐性のための、チャンク分割録音+IndexedDB

録音は`MediaRecorder`のローテーションによって約2分ごとのチャンク(`CHUNK_MS`)に分割され、各チャンクは確定するとすぐに(どこかへ送信される前に)IndexedDB(`shotokutaishi_db`、`chunks`ストアと`sessions`ストア)へ保存されます。送信・文字起こしは、ユーザーがトリミング範囲を確定した後にのみ行われます(録音中に行われることはありません)。アプリが強制終了・バックグラウンド化されたり、途中でネットワークを失ったりした場合、次回起動時に`checkForRecovery()`/`recoverSession()`が未送信のチャンクを拾い上げて再開します。送信に失敗したチャンクは破棄されず保持され(`status:'failed'`)、再送信の対象になります。このコードについて考える際は、録音〜送信の全ライフサイクルを通じてブラウザタブが生き続けることを前提にしないでください。

`CHUNK_MS`は現在2分。復旧の粒度を細かくするための設定で、リクエスト回数は増えるが、文字起こしは音声の分数で課金されるため費用は変わらない。ただし`useWhisperPrompt`を有効化する場合は、プロンプトトークンがリクエスト回数分かかる点に注意。

### トリミング/確認UI

録音後、ユーザーは波形バー(`#waveWrap`、`#trimHandleStart`/`#trimHandleEnd`)上の2つのハンドルを直接ドラッグして送信範囲を選びます — 別々のスライダーやステッパーはありません。これらのハンドルはPointer Eventsで駆動するカスタムコントロール(見た目のつまみより広い44pxのヒット領域を持つ)で、実際の値の保持・クランプは今も実体の`<input type=range>`要素(`#trimStart`/`#trimEnd`、非表示)が`normalizeTrimValues()`/`updateTrimUI()`を通じて担っています — ドラッグ・キーボードのハンドラはこれらのinputに値を書き込んで`updateTrimUI()`を呼び出しており、クランプ処理を重複実装してはいません。

### デバッグ/QA用の切り替えスイッチ

設定ゾーンの先頭付近にある`const`は、実機での確認のために一時的に切り替え、確認後に戻すことを想定しています:
- `FORCE_SHOW_ONBOARDING` — `true`の間は、閉じたかどうかに関わらず初回案内(案内A/B)が毎回のロードで表示される。オンボーディングが「完了」した状態だと思い込む前に、現在の値を確認すること — デザインレビューの後、trueのまま残しやすい。

### テストの方針

`tests/smoke.test.js`にはフレームワークもDOMもありません:正規表現で`index.html`からインラインの`<script>`本体を取り出し、末尾の`// ===== 初期化 =====`部分を、選択した内部の関数・変数を`globalThis.__trimTest`に公開するオブジェクトリテラルに置き換えたうえで、`vm`コンテキスト内で、手作りの偽の`document`/`localStorage`/`AudioContext`(テストファイル内の`element()`と`context`を参照)を相手に実行します。アサーションは`__trimTest`を呼び出します。新しい内部関数をテストする必要がある場合は、その公開オブジェクトリテラルに追加し(単純な文字列置換なので、その名前の関数・変数がIIFEのスコープ内に存在してさえいればよい)、既存のアサーションの後に新しいアサーションを追加してください — 2つ目のテストハーネスを新たに作らないこと。

## 譲れない3つの軸

このアプリの存在理由そのものなので、これらを崩す提案・実装はしないこと。
崩さざるを得ない事情が生じた場合は、実装する前に必ず確認を取ること。

1. **月額課金ゼロ、API実費のみ。** ユーザーは自分のOpenAI APIキーを設定し、
   使った分だけを直接OpenAIに支払う。中間マージンを取る仕組みは持たない。
   1件ごと・月合計のコスト概算をアプリ内に表示するのは、
   「月末にいくら請求が来るか分からない」という不安への回答であり、
   おまけ機能ではない。削らないこと。
2. **端末内保存。** 録音・文字起こし結果・商談記録・APIキーは
   ブラウザ内(IndexedDB / localStorage)にのみ保存する。
   運営側のサーバーは持たない。外部送信は、文字起こし・要約のために
   OpenAI APIへ送る分だけ。この例外は設定画面で明示的に説明済み。
   分析・ログ収集・エラー通知を含め、他の送信先を追加しないこと。
3. **アカウント登録不要。** ログイン、メールアドレス登録、
   利用規約への同意ゲートなどを追加しないこと。
   将来的に月額課金+ログイン方式へ移行する可能性はあるが、
   それは別途判断する事項であり、現行方針ではない。

## 出力の絶対ルール(捏造防止)

このアプリの出力は、保険営業担当者が会社に提出する正式な商談記録になる。
事実と異なる記載は実害を生む。要約プロンプト
(`REPORT_CONFIG.systemPrompt`)を編集する際は、以下を必ず維持すること。

- **録音に無い情報を創作しない。** 文脈から自然に補える内容であっても、
  実際に話されていなければ書かない。
- **数値は話された通りに記載する。** 丸めない、単位を換算しない、
  概算にしない。聞き取れない・不明瞭な場合は「(要確認)」と記す。
- **例え話の中身を事実として書かない。** 商談中の仮定の話
  (「もし〜だったら」「例えば〜のような方の場合」)を、
  顧客の実情として記録しない。
- **同席者は、明確に発話している人物のみを記載する。**
  話題に出ただけの人物(「妻が」「息子が」など)を同席者に含めない。

これらは要約の品質の問題ではなく、提出書類としての成立条件。
出力を「読みやすく」「自然に」する方向の改善が、
このルールと衝突する場合はルールを優先すること。

## UI設計哲学:ユーザーに判断を求めない

想定ユーザーは、商談直後の忙しい保険営業担当者。
アプリの使い方を考える余力はない前提で設計している。

- **出力フォーマットは固定。** 個人商談11項目/法人商談6項目を
  AIが自動判別する。ユーザーに「個人ですか法人ですか」と選ばせない。
- **設定項目を増やさない。** 「〜する/しない」の選択肢を足す前に、
  どちらか一方をデフォルトとして決め切れないかを検討すること。
- **モード切替は「商談」「メモ」の2つのみ**で、デフォルトは常に「商談」。
  起動のたびに「商談」へ戻す挙動を維持すること
  (メモモードのまま次の商談を録ってしまう事故を防ぐため)。
- 確認ダイアログ・注意書きを追加する前に、
  そもそも間違えようのない設計にできないかを先に考えること。

## 変更してはいけない範囲

以下は、明示的にその変更を依頼された場合を除き、触らないこと。
関連する別の作業のついでに「改善」しないこと。

- `DICTIONARY_CONFIG` の `exactReplace` / `contextNotes` / `llmGlossary`
  — 実際の誤変換報告を1件ずつ確認して積み上げた資産。
  推測で語を追加・削除しない。
- `REPORT_CONFIG.systemPrompt` / `MEMO_CONFIG.systemPrompt`
  — 上記「出力の絶対ルール」が埋め込まれている。
- IndexedDBへの保存・復旧処理(`checkForRecovery()` / `recoverSession()`、
  チャンク確定直後の保存、`status:'failed'` の保持)
  — 実際に録音が消えた事故への対策として入っている。
- `COST_CONFIG` とコスト概算の表示ロジック。
- ユーザー個人辞書(`USER_DICT` / `koememo_user_dict`)の保存形式
  — 変更するとユーザーが登録済みの語が消える。

## デザイントーン

- 白基調 + 深緑 `#2C5F5A`。ユニセックスで、上品さと信頼感を保つ。
- 黒背景は「警告」「エラー」を連想させるため、
  電池節約用の `dim-mode` 以外では使わない。
- アニメーションは、動きがあることが分かる程度に留める。
  装飾的な動き、視線を奪う動き、酔いを誘発する動き(波紋の拡散など)は不可。

## 作業の進め方

- **1つの指示書には1つの領域だけを扱う。** 複数領域を混ぜると、
  片方の実装が浅くなる。
- **「見た目・動き」に関する変更は、完成条件を「選べる状態にすること」
  までとする。** 「圧迫感がない」「もさくない」といった判定は
  実装側では行えないため、複数バリアントを実装して切り替えられる状態にし、
  最終的な選択は依頼者が行う(`LOADING_ANIMATION_VARIANT` がこの形の例)。
- **やらないことを明示された場合は厳守する。** 指示書に
  「〜は変更しない」と書かれた項目は、改善の余地が見えても触らないこと。
- `shotokutaishi-*.md` は過去の単発指示書であり、現行仕様書ではない。
  ユーザーが特定のファイルを指定した場合にのみ、その内容を要件として扱う。
